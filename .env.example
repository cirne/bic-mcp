# MCP Server Configuration

# Required: GUID used in the endpoint path /{guid}/mcp
# Generate a new GUID: node -e "console.log(require('crypto').randomUUID())"
# NOTE this GUID is not used in any real environment and just serves as an example.
MCP_GUID=db7779a6-cd30-466a-a564-00c44fd06e76

# Optional: API key for additional authentication
MCP_API_KEY=your-api-key-here

# Optional: Custom data directory (defaults to ./data)
DATA_DIR=./data
